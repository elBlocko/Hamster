/*imperative program*/void main() {
 while (vornFrei()) {
  rennBisKornAufFeld();
  sammleAllesEin();
 }   
}  // eoMain


void  rennBisKornAufFeld() {
  while (!kornDa()) {
    vor();  
  }
}
 // eom  

void sammleAllesEin() {
 while (kornDa()) {
  nimm();  
 }
}  // eom
