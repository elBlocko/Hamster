/*imperative program*/int n,m;
void main() {
 n = liesZahl("Gib die erste Zahl ein :"); 
 m = liesZahl("Gib die zweite Zahl ein :");
 
 if (n%m == 0) {
  schreib(m + " ist Teiler von " + n);
 } 
  else { schreib(m + " ist kein Teiler von " + n);
  }
}
